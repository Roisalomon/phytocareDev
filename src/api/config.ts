// src/api/config.ts
import axios from 'axios';
// import { env } from '../config/env.local';
// import { getAccessToken, getRefreshToken, setTokens, clearTokens } from '../utils/token-cookie';

// export const apiClient = axios.create({
//   baseURL: env.AUTH_API_BASE_URL,
// });

// ✅ Intercepteur de requête : ajoute Authorization
// apiClient.interceptors.request.use((config) => {
//   const token = getAccessToken();
//   if (token) {
//     config.headers.Authorization = `Bearer ${token}`;
//   }
//   return config;
// });

// ✅ Intercepteur de réponse : gère token expiré
// apiClient.interceptors.response.use(
 
// );
